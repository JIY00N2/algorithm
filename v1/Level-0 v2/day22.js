/*
2. 두 수의 합
function solution(a, b) {
    const bigA = BigInt(a);
    const bigB = BigInt(b);

    const result = (bigA + bigB).toString();
    return result;
    
}
4. 배열의 원소 삭제하기 
function solution(arr, delete_list) {
    return arr.filter(a => !delete_list.includes(a))
}
 */